# Migration `20200411014532-refactor-models`

This migration has been generated by Neto6391 at 4/11/2020, 1:45:32 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."User" (
    "cpf" text  NOT NULL ,
    "id" SERIAL,
    "nome" text  NOT NULL ,
    PRIMARY KEY ("id")
) 

CREATE TABLE "public"."Incident" (
    "descricao" text  NOT NULL ,
    "id" SERIAL,
    "titulo" text  NOT NULL ,
    PRIMARY KEY ("id")
) 

CREATE TABLE "public"."Address" (
    "bairro" text   ,
    "cep" text  NOT NULL ,
    "cidade" text  NOT NULL ,
    "estado" text  NOT NULL ,
    "id" SERIAL,
    "logradouro" text  NOT NULL ,
    "pais" text  NOT NULL ,
    PRIMARY KEY ("id")
) 

CREATE TABLE "public"."IncidentUsers" (
    "denunciaId" integer  NOT NULL ,
    "denuncianteId" integer  NOT NULL ,
    "enderecoId" integer  NOT NULL ,
    "id" SERIAL,
    "latitude" Decimal(65,30)  NOT NULL ,
    "longitude" Decimal(65,30)  NOT NULL ,
    PRIMARY KEY ("id")
) 

CREATE  INDEX "Incident.titulo_descricao" ON "public"."Incident"("titulo","descricao")

ALTER TABLE "public"."IncidentUsers" ADD FOREIGN KEY ("denuncianteId")REFERENCES "public"."User"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."IncidentUsers" ADD FOREIGN KEY ("denunciaId")REFERENCES "public"."Incident"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."IncidentUsers" ADD FOREIGN KEY ("enderecoId")REFERENCES "public"."Address"("id") ON DELETE CASCADE  ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200411014532-refactor-models
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,44 @@
+datasource db {
+  provider = "postgresql"
+  url      = env("DATABASE_URL")
+}
+
+generator client {
+  provider = "prisma-client-js"
+}
+
+model User {
+  id   Int    @default(autoincrement()) @id
+  nome String
+  cpf  String
+}
+
+model Incident {
+  id        Int    @default(autoincrement()) @id
+  titulo    String
+  descricao String
+
+  @@index([titulo, descricao])
+}
+
+model Address {
+  id         Int     @default(autoincrement()) @id
+  logradouro String
+  bairro     String?
+  cidade     String
+  estado     String
+  pais       String
+  cep        String
+}
+
+model IncidentUsers {
+  id            Int      @default(autoincrement()) @id
+  latitude      Float
+  longitude     Float
+  denuncianteId Int
+  denunciaId    Int
+  enderecoId    Int
+  denunciante   User     @relation(fields: [denuncianteId], references: [id])
+  denuncia      Incident @relation(fields: [denunciaId], references: [id])
+  endereco      Address  @relation(fields: [enderecoId], references: [id])
+}
```


